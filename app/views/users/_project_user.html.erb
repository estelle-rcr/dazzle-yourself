
<div class="container bootstrap snippet m-5">
<h2>Les projets auxquels je participe :</h2>
<% if @projects != nil %>
    <div class="row">
        <% @projects.all.each do |project| %>
        <div class="col-sm-3">
            <div class="card text-dark bg-light mb-3" style="max-width: 20rem;">
                <div class="card-header"><%= project.title%><br></div>
                <div class="card-body">
                    <h4 class="card-title"><%= project.start_date%></h4>
                </div>
            </div>
        </div>
        <% end %>
    </div>

<% else %>
<p>Vous n'êtes inscrit à aucun projet pour le moment</p>
<% end%>

</div>
