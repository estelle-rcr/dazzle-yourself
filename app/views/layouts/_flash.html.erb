<% flash.each do |type, msg| %>
    <div class="alert alert-dismissible <%= bootstrap_class_for_flash(type) %> m-0" role="alert">
        <button type="button" class="close" data-dismiss="alert">&times;</button>
      <p style="font-size: 15px;"><%= msg %></p>
    </div>
  <% end %>